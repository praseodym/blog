# The URL the site will be built for
base_url = "https://www.praseodym.net/blog"

# The site title and description; used in feeds by default.
title = "praseodym.net/blog"
description = ""

# The default language; used in feeds.
default_language = "en"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to generate a feed file for the site
generate_feed = true

# The filename to use for the feed. Used as the template filename, too.
# Defaults to "atom.xml", which has a built-in template that renders an Atom 1.0 feed.
# There is also a built-in template "rss.xml" that renders an RSS 2.0 feed.
feed_filename = "feed.xml"

# When set to "true", a feed is automatically generated for each taxonomy term.
# Available categories, tags, etc. will have their own feed.
generate_categories_pages = false
generate_tags_pages = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# Configuration for the Markdown rendering
[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "base16-ocean-dark"

# Extra options for syntax highlighting
[markdown.highlight]
extra_syntaxes_and_themes = []

# Social media and other links
[extra.footer_links]
github = "praseodym"
twitter = "praseodym"
rss = true  # Enable RSS icon in footer
